import { ViteDevServer, Plugin } from 'vite';
/**
 * Create a self-contained plugin with proper caching
 */
export declare function remoteAssetsPlugin(): Plugin & {
    processRemoteAssets: (html: string, viteServer: ViteDevServer) => Promise<string>;
};
