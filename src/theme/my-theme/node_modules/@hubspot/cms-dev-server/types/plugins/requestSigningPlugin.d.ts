import type { Plugin } from 'vite';
/**
 * Plugin that provides URL signing capabilities
 * Uses internal signing mechanisms when available, with fallback for public environments
 */
export default function requestSigningPlugin(): Plugin;
